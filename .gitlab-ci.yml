pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html app.slim.js app.css public/
    - cp -r vendor obq_index public/
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
